#!/bin/sh
# agentic-board guard — final backstop before content leaves the machine.
if command -v pwsh >/dev/null 2>&1; then PS=pwsh; else PS=powershell; fi
"$PS" -NoProfile -ExecutionPolicy Bypass -File scripts/guard-no-private.ps1 -Mode push
exit $?
