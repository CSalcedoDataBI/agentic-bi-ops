# editor / os
.DS_Store
Thumbs.db
*.log
# local scratch
/tmp/
/.local/
# local-only private denylist — MUST NEVER be committed
.abios/private-denylist.txt
# internal dev scaffolding (kept local, not shipped in the public plugin)
/docs/
# board backups (may contain private board data) — never commit
.agentic-bi-ops/
**/.backups/
*.items.json
*.project.json
*.fields.json
/HANDOFF.md
/.handoffs/
